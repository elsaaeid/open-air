// Importing components from TypeScript files
import Navbar from "./Navbar";      // Ensure Navbar.tsx exists
import Sidebar from "./Sidebar";    // Ensure Sidebar.tsx exists
import FormField from "./FormField"; // Ensure FormField.tsx exists
import Loader from "./Loader";       // Ensure Loader.tsx exists
import PostCard from "./PostCard";   // Ensure PostCard.tsx exists

// Exporting the components
export {
    Navbar,
    Sidebar,
    FormField,
    Loader,
    PostCard,
};